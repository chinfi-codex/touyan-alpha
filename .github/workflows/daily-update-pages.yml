name: daily-update-pages

on:
  workflow_dispatch:
  schedule:
    - cron: '10 23 * * *'
    - cron: '10 14 * * *'

permissions:
  contents: write

jobs:
  collect-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "event=${{ github.event_name }}"
